# Streamlit Basic Practice

Streamlit을 활용해 기본 UI 컴포넌트, 데이터 표시, 차트 시각화, 인터랙티브 필터, 파일 업로드 등 핵심 기능을 실습한 프로젝트입니다.

---

## 📁 Project Asignment
| 멤버 | 업무 | 
|---|---|
|HwaYoum|task 1,2|
|junha-research|task 4|
|kkangwood|task 3|
|waitinghm|task 5|

---

## 📗 How to do
1. git clone ~~
2. pip install -r requirement.txt
3. streamlit run ./main.py

## 📌 Features

### 🧩 Task 1 — 기본 UI 컴포넌트
이름, 나이, 좋아하는 색을 지정해서 제출할 수 있습니다
- 텍스트 입력 (`st.text_input`)
- 슬라이더 (`st.slider`)
- 선택 박스 (`st.selectbox`)
- 체크박스 (`st.checkbox`)
- 제출 버튼 (`st.button`) → 제출 완료 메시지 출력
  
---

### 📊 Task 2 — 데이터 표시
`penguins.csv` 데이터를 빠르게 확인해볼 수 있습니다
- `penguins.csv` 데이터를 불러와 `st.dataframe()`으로 상위 5개 행 출력
---

### 📈 Task 3 — 차트 시각화
`penguins.csv` 데이터를 기반으로 다음 차트를 생성합니다.
- Line Chart — bill_length_mm  
- Bar Chart — body_mass_g  
- Area Chart — flipper_length_mm
---

### 🔍 Task 4 — 인터랙티브 데이터 필터
컬럼 타입에 따라 자동으로 필터 UI를 생성합니다.
- **범주형**: 멀티셀렉트
- **숫자형**: 슬라이더
- **날짜형**: 기간 선택
- **문자열**: 부분 문자열 및 정규식 검색

필터링된 데이터를 `st.dataframe()`으로 출력합니다.

---

### 📂 Task 5 — 파일 업로드
업로드된 CSV 파일로:

- 상위 5개 데이터 행  
- 기본 통계 요약  

을 확인할 수 있습니다.

---
